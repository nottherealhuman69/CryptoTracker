<!-- views/index.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crypto Price Tracker</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        .logout-container {
            position: absolute;
            top: 20px;
            left: 20px;
        }

        .logout-btn {
            background-color: #dc3545;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s;
        }

        .logout-btn:hover {
            background-color: #c82333;
        }

        /* Ensure the existing button container doesn't overlap */
        .button-container {
            margin-top: 60px;
        }

        h1 {
            margin-top: 80px; /* Add space for logout button */
        }
    </style>
</head>
<body>
    <!-- Logout Button -->
    <div class="logout-container">
        <button class="logout-btn" onclick="window.location.href='/logout'">Logout</button>
    </div>

    <h1>Welcome to the Crypto Price Tracker!</h1>

    <!-- Navigation Buttons -->
    <div class="button-container">
        <button onclick="window.location.href='/cryptos'">Go to Crypto Prices</button>
        <button onclick="window.location.href='/stats'">Go to Stats</button>
        <button onclick="window.location.href='/deviation'">Go to Standard Deviation</button>
    </div>
</body>
</html>